
@(Html.Kendo().Grid<Number2.Models.ViewModel>()
              .Name("grid")
              .Columns(columns =>
              {
                  /*
                  columns.Bound(c => c.BookId);
                  columns.Bound(c => c.BookName);
                  columns.Bound(c => c.Content);
                  columns.Bound(c => c.Pages);
                  columns.Bound(c => c.AllAuthors);
    */
                  columns.Bound(c => c.Book);
                  columns.Bound(c => c.Authors);
              })
              .NoRecords("No records found.")
              .Scrollable()
              .DataSource(dataSource => dataSource
                  .Ajax()
                  .Read(read => read.Action("Books_Read", "Grid"))
                  .Model(model =>
                  {

                      //model.Id(book => book.BookId);

                      //Declare a model field and optionally specify its default value (used when a new model instance is created).
                      //model.Field(product => product.ProductName).DefaultValue("N/A");
                  })
              )
)
<script>
    $(function() {
        //Notice that the Name() of the Grid is used to get its client-side instance.
        var grid = $("#grid").data("kendoGrid");
        
    });
</script>
